version: "3"

services:
  docd:
    build: ./_docd
    networks:
      - intra
  dev:
    build: .
    volumes:
      - ${PWD}:/go/src/github.com/Zenika/rao/rao-back
      - ${PWD}/_dist:/_dist
    env_file:
      - .env
    environment:
      - MODE=${MODE}
networks:
  intra:
